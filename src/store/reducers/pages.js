const initialState = {
	home: {
		title: "Push.js Notifications"
	}
};

const pagesReducer = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export default pagesReducer;